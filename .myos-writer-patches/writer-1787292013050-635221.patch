diff --git a/bin/myos-dispatch-hook b/bin/myos-dispatch-hook
index 3bf2dd9..3fb59b8 100755
--- a/bin/myos-dispatch-hook
+++ b/bin/myos-dispatch-hook
@@ -970,6 +970,24 @@ function handleUserPromptSubmit(payload, surface, options = {}) {
   const text = readPromptText(payload);
   if (!text) return null;
 
+  if (/^h$/i.test(text.trim())) {
+    appendRouteLog({
+      at: new Date().toISOString(),
+      surface,
+      hookEventName: "UserPromptSubmit",
+      targetKind: "user_prompt",
+      inputHash: `sha256:${sha256(text)}`,
+      inputLength: text.length,
+      cwd: safeString(payload.cwd || process.cwd()),
+      route: "ping_shortcut",
+      capabilityId: null,
+      branch: null,
+      lanesAutoDispatched: 0,
+      agentCallsObserved: 0,
+    });
+    return { decision: "block", reason: "ping" };
+  }
+
   const sessionId = readSessionId(payload);
   const homeRoot = preferredHomeRoot();
   const sidecarResultsDir = path.join(homeRoot, "state", "sidecar-results", sessionId);
diff --git a/test/myos-dispatch-native-hook.test.js b/test/myos-dispatch-native-hook.test.js
index 0d8af41..7a03507 100644
--- a/test/myos-dispatch-native-hook.test.js
+++ b/test/myos-dispatch-native-hook.test.js
@@ -56,6 +56,53 @@ test("UserPromptSubmit emits compact MyOS Dispatch route context", () => {
   assert.match(context, /Mandatory behavior:/);
 });
 
+test("UserPromptSubmit blocks uppercase H ping", () => {
+  const output = handleHookPayload({
+    hook_event_name: "UserPromptSubmit",
+    cwd: process.cwd(),
+    prompt: "H",
+  }, "test");
+
+  assert.equal(output.decision, "block");
+  assert.equal(output.reason, "ping");
+  assert.equal(output.hookSpecificOutput, undefined);
+});
+
+test("UserPromptSubmit blocks lowercase h ping", () => {
+  const output = handleHookPayload({
+    hook_event_name: "UserPromptSubmit",
+    cwd: process.cwd(),
+    prompt: "h",
+  }, "test");
+
+  assert.equal(output.decision, "block");
+  assert.equal(output.reason, "ping");
+  assert.equal(output.hookSpecificOutput, undefined);
+});
+
+test("UserPromptSubmit does not over-match ordinary prompts", () => {
+  const output = handleHookPayload({
+    hook_event_name: "UserPromptSubmit",
+    cwd: process.cwd(),
+    prompt: "Hello",
+  }, "test");
+
+  assert.notEqual(output.decision, "block");
+  assert.ok(output.hookSpecificOutput?.additionalContext);
+});
+
+test("UserPromptSubmit blocks whitespace-padded H ping", () => {
+  const output = handleHookPayload({
+    hook_event_name: "UserPromptSubmit",
+    cwd: process.cwd(),
+    prompt: " H ",
+  }, "test");
+
+  assert.equal(output.decision, "block");
+  assert.equal(output.reason, "ping");
+  assert.equal(output.hookSpecificOutput, undefined);
+});
+
 test("PreToolUse (Claude surface) routes Bash commands and preserves RTK rewrite", () => {
   const output = handleHookPayload({
     hook_event_name: "PreToolUse",
